#include "claves.h"

/*
 * app_test_red.c — Cliente mínimo para pruebas de error de red.
 * Realiza una única llamada que siempre requiere conexión al servidor.
 * Devuelve 0 si la conexión tuvo éxito, distinto de 0 en caso contrario.
 */
int main() {
    return destroy();
}